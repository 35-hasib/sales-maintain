{
  "installCommand": "npm install && npm --prefix backend install && cd backend && npx prisma generate && npx prisma migrate deploy",
  "buildCommand": "cd frontend && npm install && npm run build",
  "outputDirectory": "frontend/dist",
  "framework": "vite",
  "regions": ["sin1"],
  "functions": {
    "api/index.js": {
      "maxDuration": 10
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index.js" },
    { "source": "/((?!api|assets/).*)", "destination": "/index.html" }
  ],
  "crons": [
    { "path": "/api/health", "schedule": "*/5 * * * *" }
  ]
}