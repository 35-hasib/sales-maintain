name: Backend Keep Alive

on:
  schedule:
    - cron: "3,13,23,33,43,53 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping backend
        run: |
          curl -f --max-time 30 \
            https://sales-maintain-iisi1.vercel.app/api/health
